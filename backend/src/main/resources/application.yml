server:
  port: 8080

spring:
  application:
    name: cavacamisa-backend

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
